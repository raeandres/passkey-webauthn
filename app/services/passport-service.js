class PassportService {
    init(store){
        // 1. Configure passport to use WebAuthn Strategy
        // 2. passport serialize error
        // 3. passport deserialize user
    }
}

module.exports = PassportService